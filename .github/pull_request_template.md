## Summary

<!-- What does this PR do? One sentence is enough for small changes. -->

## Phase / task

<!-- Which phase or task does this implement? e.g. "Phase 12 — Treaty rate verification" -->

## Checklist

- [ ] `npm run build` passes — zero TypeScript errors
- [ ] `npm test` passes — all tests green
- [ ] New environment methods have a simulate fallback
- [ ] New tools have parameter validation + `{ error: ... }` return for bad input
- [ ] New tools have tests: happy path, error path, regression guard
- [ ] `reports/` and `data/registry.json` are gitignored (not staged)
- [ ] No API keys committed

## Test notes

<!-- How was this tested? Which entry point (CLI / web UI)? Which input file? -->

## Known limitations / follow-up

<!-- Anything intentionally left out of scope for this PR? -->
